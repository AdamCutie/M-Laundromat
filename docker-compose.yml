services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: laundromat_v2
    restart: always
    ports:
      - "5000:5000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
